namespace TBG
{
    public class Sorter
    {
        private string _name;
        public int _points;
        private int _id;
        public Sorter(string name, int points, int id)
        {
            this._name = name;
            this._points = points;
            this._id = id;
        }
    }
}